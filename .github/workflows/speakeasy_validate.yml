name: Validate Speakeasy Generation

# This workflow validates that the Speakeasy-generated code compiles correctly.
# It runs on every PR push but does NOT create PRs or push changes.
# For actual regeneration, use the manual "Generate" workflow.

on:
  pull_request:
    paths-ignore:
      - 'README.md'
      - 'docs/**'
      - 'examples/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: Validate Generation
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
          cache: true

      - name: Install Speakeasy CLI
        run: |
          curl -fsSL https://raw.githubusercontent.com/speakeasy-api/speakeasy/main/install.sh | sh
          echo "$HOME/.speakeasy/bin" >> $GITHUB_PATH

      - name: Clean regeneration experiment
        run: |
          # Delete ALL generated code to force fully clean regeneration
          # This tests if the mismatch is caused by preserved/stale files
          echo "Removing internal/sdk for clean regeneration..."
          rm -rf internal/sdk
          echo "Removing generated provider glue files..."
          rm -rf internal/provider/*_sdk.go
          rm -rf internal/provider/types/
          echo "Listing remaining provider files..."
          ls internal/provider/ | head -20
          echo "Done. Generated code removed."

      - name: Run Speakeasy Generation (dry-run)
        id: generate
        continue-on-error: true
        env:
          SPEAKEASY_API_KEY: ${{ secrets.SPEAKEASY_API_KEY }}
        run: |
          # Run generation but don't commit/push anything
          speakeasy run --skip-compile 2>&1 | tee speakeasy_output.txt
          echo "speakeasy_exit_code=$?" >> $GITHUB_OUTPUT

      - name: Check for generation errors
        if: steps.generate.outcome == 'failure'
        run: |
          echo "::warning::Speakeasy generation encountered issues. See logs above."
          cat speakeasy_output.txt

      - name: Debug - Inspect generated files
        if: always()
        run: |
          echo "=== Check if destinationastra.go has generated code banner ==="
          head -20 internal/sdk/models/shared/destinationastra.go || echo "File not found"
          echo ""
          echo "=== UnmarshalJSON signature in utils/json.go ==="
          grep -n "func UnmarshalJSON" internal/sdk/internal/utils/json.go || echo "Not found"
          echo ""
          echo "=== First UnmarshalJSON call in destinationastra.go ==="
          grep -n "UnmarshalJSON" internal/sdk/models/shared/destinationastra.go | head -5 || echo "Not found"
          echo ""
          echo "=== Count of old call pattern (passing bool as 5th arg) ==="
          grep -rn "UnmarshalJSON(data, &.*false, true)" internal/sdk/models/ | wc -l || echo "0"
          grep -rn "UnmarshalJSON(data, &.*false, false)" internal/sdk/models/ | wc -l || echo "0"
          echo ""
          echo "=== Sample of old call patterns (first 10) ==="
          grep -rn "UnmarshalJSON(data, &.*false, true)" internal/sdk/models/ | head -10 || echo "None found"
          echo ""
          echo "=== Count of new call pattern (passing []string as 5th arg) ==="
          grep -rn 'UnmarshalJSON(data, &.*\[\]string' internal/sdk/models/ | wc -l || echo "0"
          echo ""
          echo "=== Git diff summary ==="
          git diff --stat || true
          echo ""
          echo "=== Changed files in internal/sdk/models/shared (full list) ==="
          git diff --name-only -- internal/sdk/models/shared/ || true

      - name: Upload debug artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: speakeasy-debug
          path: |
            internal/sdk/internal/utils/json.go
            internal/sdk/models/shared/destinationastra.go
            speakeasy_output.txt
            .speakeasy/gen.lock
          retention-days: 5

      - name: Update dependencies
        run: |
          go mod tidy

      - name: Build generated code
        run: |
          go build -v ./...

      - name: Run linters
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          args: --timeout 5m

      - name: Check for uncommitted changes
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "::warning::Generation produced uncommitted changes:"
            git diff --stat
            echo ""
            echo "If these changes are expected, run 'speakeasy run' locally and commit the results."
          fi
